# Базовый URL backend API (без завершающего слэша) — для production/preview
VITE_API_URL=http://localhost:8000

# Dev: порт прокси Vite → API (если 8000 занят, поставь тот же порт, что HOST_API_PORT в backend/.env)
VITE_DEV_API_PORT=8000
